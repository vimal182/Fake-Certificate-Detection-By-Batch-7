create a blank file named "_pycache_" on every folder if error occurs.
place ai_forensic_engine.py, gradcam_utils.py,model_loader.py,ocr_text_detector.py,seal_detector.py,signature_detector.py,text_tamper.py,verification_engine.py on new folder named "ai".
place block.py, chain.py, crypto_utils.py, ledger.json, merkle on a new folder named "blockchain".
place mobilenet_forgery.onnx on a new folder named "models".
place qr_generator.py on a new folder named "qr".
place certificates.db,institution.db,main.db,recruiter.db,student.db on new folder named "database"
install tesseract using "pip install pytesseract". also download and install tesseract.exe. with path on c drive.
use python 3.10. 
create venv (virtual environment).
activate venv.
install dependencies using "pip install -r requirements.txt".
finally run "run_project.bat" file.
